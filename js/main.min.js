"use strict";

// Slider People
new Splide('#people_slider', {
  pagination: false,
  perMove: 1,
  perPage: 1,
  type: 'loop'
}).mount(); // Slider Partners

new Splide('#partners_slider', {
  arrows: false,
  autoplay: true,
  drag: false,
  gap: 30,
  interval: 2000,
  pagination: false,
  pauseOnHover: false,
  pauseOnFocus: false,
  perMove: 1,
  perPage: 4,
  type: 'loop',
  breakpoints: {
    1023: {
      perPage: 3
    },
    600: {
      perPage: 2
    }
  }
}).mount(); // Slider Benefits

new Splide('#benefits_slider', {
  gap: 30,
  pagination: false,
  perMove: 4,
  perPage: 5,
  type: 'slide',
  breakpoints: {
    1023: {
      perMove: 2,
      perPage: 2
    },
    600: {
      perMove: 1,
      perPage: 1,
      padding: {
        right: '65px'
      }
    }
  }
}).mount(); // Slider Geography

new Splide('#geography_slider', {
  pagination: true,
  perMove: 1,
  perPage: 1,
  type: 'loop',
  breakpoints: {
    1023: {
      pagination: false
    }
  }
}).mount(); // Counters are Used with Animations on Scroll
// function animate(obj, initVal, lastVal, duration) {
//   let startTime = null;
//   let currentTime = Date.now();
//   const step = currentTime => {
//     if (!startTime) {
//       startTime = currentTime;
//     }
//     const progress = Math.min((currentTime - startTime) / duration, 1);
//     obj.innerHTML = Math.floor(progress * (lastVal - initVal) + initVal);
//     if (progress < 1) {
//       window.requestAnimationFrame(step);
//     } else {
//       window.cancelAnimationFrame(window.requestAnimationFrame(step));
//     }
//   };
//   window.requestAnimationFrame(step);
// }
// const counter01 = document.getElementById('counter_01');
// const counter02 = document.getElementById('counter_02');
// const counter03 = document.getElementById('counter_03');
// let counter01Start = (function () {
//   let executed = false;
//   return function () {
//     if (!executed) {
//       executed = true;
//       animate(counter01, 0, 35, 500);
//     }
//   };
// })();
// let counter02Start = (function () {
//   let executed = false;
//   return function () {
//     if (!executed) {
//       executed = true;
//       animate(counter02, 0, 425, 1500);
//     }
//   };
// })();
// let counter03Start = (function () {
//   let executed = false;
//   return function () {
//     if (!executed) {
//       executed = true;
//       animate(counter03, 0, 785, 1000);
//     }
//   };
// })();
// Animations on Scroll

const animItems = document.querySelectorAll('.animItem');

if (animItems.length > 0) {
  window.addEventListener('scroll', animOnScroll);

  function animOnScroll(params) {
    for (let index = 0; index < animItems.length; index++) {
      const animItem = animItems[index];
      const animItemHeight = animItem.offsetHeight;
      const animItemOffset = offset(animItem).top; // const animStart = 4;

      const animStart = 2;
      let animItemPoint = window.innerHeight - animItemHeight / animStart;

      if (animItemHeight > window.innerHeight) {
        animItemPoint = window.innerHeight - window.innerHeight / animStart;
      }

      if (pageYOffset > animItemOffset - animItemPoint && pageYOffset < animItemOffset + animItemHeight) {
        animItem.classList.add('active');
      } else {
        if (!animItem.classList.contains('noRepeat')) {
          animItem.classList.remove('active');
        }
      }
    }
  }

  function offset(el) {
    const rect = el.getBoundingClientRect(),
          scrollLeft = window.pageXOffset || document.documentElement.scrollLeft,
          scrollTop = window.pageYOffset || document.documentElement.scrollTop;
    return {
      top: rect.top + scrollTop,
      left: rect.left + scrollLeft
    };
  }

  setTimeout(() => {
    animOnScroll();
  }, 400);
} // Popups Handling


const popups = new HystModal({
  linkAttributeName: 'data-hystmodal'
});